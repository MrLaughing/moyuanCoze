<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- 允许 ip-api.com 的明文 HTTP（免费版不支持 HTTPS） -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">ip-api.com</domain>
    </domain-config>
</network-security-config>
